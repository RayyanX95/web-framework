{
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel html.index",
    "start": "concurrently npm:start:*"
  },
  "devDependencies": {
    "typescript": "^4.3.2"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
